/**
 * @file Inspect vacío de configuración VS Code para mocks de tests.
 */

/** Resultado de `inspect()` sin valores en ningún ámbito (sin `null` ni `undefined`). */
export const emptyConfigurationInspect = {};
